[package]
name = "icecutter"
version = "1.4.1"
rust-version = "1.91.0"
edition = "2024"

[dependencies]
arboard = "3.6.1"
iced = "0.14.0"
rfd = "0.16.0"

# optimize for file size in the release
[profile.release]
opt-level = 's'
lto = true
codegen-units = 1
